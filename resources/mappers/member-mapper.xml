<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberMapper">
	<insert id="insertMember">
		INSERT INTO MEMBER_TBL VALUES(#{memberId}, #{memberPw}, #{memberPw2}, #{memberName}, #{memberNickname}, #{memberBirthday}, #{memberPhone}, #{memberEmail}, #{memberAddress}, DEFAULT, DEFAULT, DEFAULT)
	</insert>
	<update id="updateMember">
		UPDATE MEMBER_TBL SET MEMBER_PW = #{memberPw}, MEMBER_PW2 = #{memberPw2}, MEMBER_NICKNAME = #{memberNickname}, MEMBER_PHONE = #{memberPhone}, MEMBER_EMAIL = #{memberEmail},MEMBER_ADDRESS = #{memberAddress}, DEFAULT
	</update>
	<delete id="deleteMember">
		DELETE FROM MEMBER_TBL WHERE MEMBER_ID = #{memberId}
	</delete>

</mapper>